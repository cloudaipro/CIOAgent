claude-agent-sdk>=0.2.87
httpx
openai
pyyaml
ruamel.yaml          # round-trip YAML for dashboard Configure tab (preserves comments); pyyaml is the fallback if absent
python-telegram-bot>=22.0
pandas>=2.0
numpy<2.3              # capped: numba (via pandas_ta) requires numpy>=1.24,<2.3
matplotlib>=3.7
python-dotenv>=1.0
APScheduler>=3.10
# MemCore memory/context layer (all required — hybrid recall has no FTS-only mode)
fastembed>=0.8        # local ONNX embeddings (bge-base-en-v1.5, 768-dim), no API key
sqlite-vec>=0.1.9     # in-SQLite vector storage + ANN
tiktoken>=0.13        # local token counting for the injection budget
# Stock subsystem (cfo/stock): fetch + cache + strategies, reused from StockPricePrediction.
# Requires Python >=3.12 (pandas_ta 0.4.x).
yfinance>=0.2                  # Yahoo Finance price/volume fetch
joblib>=1.3                    # per-symbol pickle cache
pandas-market-calendars>=4.0   # NYSE trading-day alignment
pandas_ta>=0.4.71b0            # technical-analysis engine used by the strategies
finta>=1.3                     # indicators.py helper (find_swings etc.)
scikit-learn>=1.3              # pvt_strategy scoring
# IBKR live portfolio via TWS API (read-only; see cio/data/ibkr.py)
ib_async>=2.0          # community continuation of ib_insync (BSD-2, ib-api-reloaded)
# PDF report rendering (committee output)
weasyprint                     # HTML → PDF with CJK font support
markdown                       # Markdown → HTML (tables/fenced_code/sane_lists exts)
opencc-python-reimplemented    # guarantee Traditional (Taiwan) for zh committee reports
# Indicator visualization (指標視覺化, cio/stock/viz) — PNG path needs nothing
# beyond matplotlib (already above). bokeh is OPTIONAL: only the interactive HTML
# dashboard chart uses it (no selenium/webdriver — PNG export is matplotlib's job).
bokeh>=3.4                     # optional: cio/stock/viz interactive HTML adapter
