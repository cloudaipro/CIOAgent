# Copy to .env and fill in. Get a token from @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=

# Optional: pin the model the agent uses (defaults to Claude Code's default).
# CFO_MODEL=claude-haiku-4-5

# Daily digest time (local timezone). Set CFO_DIGEST_HOUR=off to disable.
# CFO_DIGEST_HOUR=8
# CFO_DIGEST_MINUTE=0
